# Fitness v1 release acceptance

Finish the synthetic data-engineering product: validated scheduled export ingestion; point-in-time session history; daily and weekly summaries; dbt checks; Airflow interval/retry/backfill; failure alert transport; authenticated summary API and dashboard; reproducible containers; hosted CI; Terraform-managed on-demand AWS execution and recovery evidence; recruiter-readable release and portfolio links.

Use AWS us-east-1, personal portfolio profile. Target negligible idle compute and under $5/month for light demonstrations, shared with the existing project budget. No always-on VM, database, NAT gateway or load balancer. No automatic recurring demo runs. Retained image/object/log storage has small usage charges. Native wearable sync, clinical recommendations, richer strength-set/goal tracking and AI plan generation remain explicitly beyond v1.
